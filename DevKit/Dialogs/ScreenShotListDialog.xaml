<UserControl
    x:Class="DevKit.Dialogs.ScreenShotListDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:vm="clr-namespace:DevKit.ViewModels"
    Width="235"
    Height="400"
    d:DataContext="{d:DesignInstance Type=vm:ScreenShotListDialogViewModel}"
    Background="{DynamicResource DevKitBackgroundBrush}"
    mc:Ignorable="d">

    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterOwner" />
            <Setter Property="ShowInTaskbar" Value="False" />
            <Setter Property="SizeToContent" Value="WidthAndHeight" />
            <Setter Property="ResizeMode" Value="NoResize" />
        </Style>
    </prism:Dialog.WindowStyle>

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="10"
            HorizontalAlignment="Center"
            Text="请选择需要导出到电脑的截屏" />

        <ListBox
            x:Name="ScreenshotsListBox"
            Grid.Row="1"
            Margin="10,0"
            BorderBrush="{DynamicResource DevKitBorderBrush}"
            ItemsSource="{Binding Screenshots}"
            ScrollViewer.CanContentScroll="False">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding ItemDoubleClickedCommand}" CommandParameter="{Binding SelectedItem, ElementName=ScreenshotsListBox}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Text="{Binding FilePath}"
                            TextWrapping="Wrap" />

                        <Button
                            Grid.Column="1"
                            Margin="5,0,0,0"
                            Command="{Binding DataContext.DeleteScreenshotCommand, ElementName=ScreenshotsListBox}"
                            CommandParameter="{Binding Path=SelectedItem, ElementName=ScreenshotsListBox}"
                            Content="&#xe692;"
                            FontFamily="/DevKit;component/Fonts/#iconfont"
                            Foreground="White">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border
                                                    x:Name="ButtonContainerBorder"
                                                    Padding="2"
                                                    CornerRadius="8">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>

                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ButtonContainerBorder" Property="Background" Value="Red" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="1" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                <Border
                                    x:Name="ItemContainerBorder"
                                    Padding="5,10"
                                    BorderBrush="#F1F1F1"
                                    BorderThickness="1"
                                    CornerRadius="3">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ItemContainerBorder" Property="Background" Value="#F8F8F8" />
                                        <Setter Property="TextBlock.Foreground" Value="{StaticResource PrimaryBrush}" />
                                    </Trigger>

                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="ItemContainerBorder" Property="Background" Value="#E7F4FF" />
                                        <Setter TargetName="ItemContainerBorder" Property="TextBlock.Foreground" Value="{StaticResource PrimaryBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <UniformGrid
            Grid.Row="2"
            Margin="10"
            Rows="1">
            <Button
                Margin="0,0,5,0"
                HorizontalAlignment="Stretch"
                Command="{Binding SortScreenshotsCommand}"
                Content="排序"
                Style="{StaticResource ButtonInfo}" />

            <Button
                Margin="5,0,0,0"
                HorizontalAlignment="Stretch"
                Command="{Binding OutputScreenshotCommand}"
                CommandParameter="{Binding ElementName=ScreenshotsListBox, Path=SelectedItem}"
                Content="导出到电脑"
                Style="{StaticResource ButtonPrimary}" />
        </UniformGrid>
    </Grid>
</UserControl>